// TODO: Maybe it would be better to put this into a .env file?
export const SUBPATH = `${process.env.REACT_APP_SUBPATH}`
export const API_BASE = `${process.env.REACT_APP_API_URL}${process.env.REACT_APP_API_BASE}`
